@* ------------------------------------------------------------------------------------------------
<copyright file="ConfirmDialog.razor" company="Starion Group S.A.">

  Copyright 2026 Starion Group S.A.
  SPDX-License-Identifier: Apache-2.0

</copyright>
------------------------------------------------------------------------------------------------ *@

@inherits BloomComponentBase

<ModalShell IsOpen="@(this.IsOpen)"
            IsOpenChanged="this.IsOpenChanged"
            Title="@(this.Title)"
            Size="ModalSize.Small"
            ShowCloseButton="false"
            CloseOnBackdropClick="false"
            Class="@(this.GetCssClass())"
            @attributes="this.AdditionalAttributes">
    <ChildContent>
        <div class="mb-confirm-dialog__body">
            <span class="@(this.GetIndicatorCssClass())" aria-hidden="true">
                @(this.GetIndicatorText())
            </span>

            @if (!string.IsNullOrWhiteSpace(this.Description))
            {
                <p class="mb-confirm-dialog__description">@(this.Description)</p>
            }
        </div>
    </ChildContent>

    <FooterContent>
        <div class="mb-confirm-dialog__footer">
            <Button Variant="ButtonVariant.Secondary"
                    Size="ButtonSize.Small"
                    Disabled="@(this.IsActionInProgress())"
                    OnClick="this.HandleCancelAsync">
                @(this.CancelText)
            </Button>

            <Button Variant="@(this.GetConfirmButtonVariant())"
                    Size="ButtonSize.Small"
                    Disabled="@(this.IsActionInProgress())"
                    IsLoading="@(this.IsActionInProgress())"
                    OnClick="this.HandleConfirmAsync">
                @(this.ConfirmText)
            </Button>
        </div>
    </FooterContent>
</ModalShell>
